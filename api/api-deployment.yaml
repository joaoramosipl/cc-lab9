apiVersion: apps/v1
kind: Deployment
metadata:
  name: api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api
  template:
    metadata:
      labels:
        app: api
    spec:
      containers:
        - name: api
          image: joaoramosipl/api:latest
          ports:
            - containerPort: 8080
          envFrom:
            - secretRef:
                name: api-secret
            - configMapRef:
                name: api-config